{
  "private": true,
  "engines": {
    "yarn": "<2.0.0",
    "node": ">=14.0.0"
  },
  "workspaces": {
    "packages": [
      "packages/*",
      "examples/*",
      "site"
    ]
  },
  "scripts": {
    "dev:site": "",
    "dev:example:blog": "cd examples/theme-blog && npm run serve",
    "dev:example:doc": "",
    "dev:example:book": "",
    "build:site": "",
    "deploy:site": "",
    "clean": "npm run clean:cache && rimraf {.,site}/node_modules {packages,examples}/*/node_modules",
    "clean:cache": "rimraf {.,.site}/{.cache,public} {packages,examples}/*/{.cache,public}"
  },
  "devDependencies": {
    "lerna": "^3.22.1",
    "prettier": "^2.2.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.1.3"
  },
  "resolutions": {
    "postcss": "^8.2.4"
  }
}
